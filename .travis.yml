language: node_js
  node_js:
    - 12
before_install:
  - sudo npm install -g expo-cli
install:
  - npm install
script:
  - expo build:web
  - echo $(ls)
deploy:
  local_dir: 
  provider: pages
  skip_cleanup: true
  github_token: $PAGES_TOKEN 
  keep_history: true
  on:
    branch: main
